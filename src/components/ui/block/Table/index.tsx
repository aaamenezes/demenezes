import Body from './Body';
import Footer from './Footer';
import Header from './Header';
import Root from './Root';

const Table = Object.assign(Root, { Header, Body, Footer });

export default Table;
